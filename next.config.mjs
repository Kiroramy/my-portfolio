/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
    output: "export",
  basePath: "/my-portfolio",
  assetPrefix: "/my-portfolio/",
};

export default nextConfig;
